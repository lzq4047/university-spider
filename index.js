const request = require('request');
const cheerio = require('cheerio');
const fs = require('fs');
const baseUrl = 'http://www.gaokaopai.com/daxue-0-0-0-0-0-0-0.html'
const start = () => {
  console.log('start fetch...' + baseUrl);
}

start()
